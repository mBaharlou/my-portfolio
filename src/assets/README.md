Place your real profile photo here when ready.

Suggested filename:
- `profile-photo.jpg`

Then update the hero section in `src/components/home/HeroSection.jsx` to render the image instead of the placeholder card.
